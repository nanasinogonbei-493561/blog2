# AGENTS.md

このリポジトリは、技術ブログの記事制作・レビュー・WordPress投稿をCodexで行うための専用プロジェクトです。

## 1. ブログの目的

エンジニアや技術に関心のある読者が、実際に手を動かせる情報を提供します。

主なテーマ:
- AI / Codex / ChatGPT
- WordPress / Web運用
- Python / API / 自動化
- 個人開発
- 業務効率化
- 技術系副業

記事の評価基準:
1. 読者の疑問に答えている
2. 実際に再現できる
3. 読後に次の行動が分かる

## 2. 最重要ルール

- 明示的な指示がない限りWordPressへの投稿は `draft`。
- 「公開して」「publishして」と明示されない限り公開しない。
- `.env`、Application Password、APIキー、トークン、Cookie、秘密鍵などを出力・コミットしない。
- 実際の秘密情報を記事例に使わない。
- 既存記事の `wordpress_post_id`、`wordpress_url`、`wordpress_featured_media_id` を不用意に削除しない。
- `wordpress_post_id` がある記事は新規作成ではなく既存投稿を更新する。
- 事実関係に自信がない場合は推測で埋めない。
- 仕様、価格、機能、UI、法律、バージョンなど変化する情報は可能なら一次情報を確認する。
- 本文にH1を書かない。WordPressの投稿タイトルをH1とみなす。
- 不要な前置き、過剰な煽り、意味の薄い一般論で文字数を水増ししない。

## 3. ディレクトリ

- `articles/drafts/`: 執筆中・WordPress下書き中
- `articles/published/`: 公開済み
- `config/blog.yaml`: ブログ・投稿設定
- `prompts/article.md`: 執筆ルール
- `prompts/review.md`: レビュールール
- `scripts/wp_publish.py`: 投稿・更新
- `scripts/wp_media.py`: メディアアップロード
- `scripts/wp_categories.py`: カテゴリ・タグ管理

## 4. 記事の基本方針

### 結論を先に書く

冒頭で、この記事が何を解決するか、何ができるようになるかを明示する。

悪い例:
> 最近AIが話題です。さまざまなツールがあります。

良い例:
> CodexからWordPressへ自動投稿するなら、WordPress REST APIとApplication Passwordを使う方法が扱いやすいです。

### 実例を優先する

抽象説明の後には、できるだけ以下を入れる。
- コマンド
- 設定例
- コード
- ディレクトリ構成
- 入力例
- 実行結果
- エラー例と原因

### 読者の失敗を先回りする

技術記事では成功手順だけでなく、次も重視する。
- どこで詰まりやすいか
- どのエラーが出るか
- 原因は何か
- 何を確認すればよいか

必要なら「よくあるエラー」や「トラブルシューティング」を入れる。

## 5. 文体

- 日本語
- です・ます調
- 率直で簡潔
- 断定できることは曖昧にしない
- 分からないことを分かったふりをしない
- 一文を必要以上に長くしない
- 同じ意味の説明を繰り返さない
- 「簡単です」「誰でもできます」を乱用しない
- 「完全」「最強」「革命的」などの煽り表現を避ける

初心者向けでも読者を子ども扱いしない。
専門用語は必要なら最初に短く説明する。

## 6. 記事構成

基本形:
1. 導入
2. 結論または完成形
3. 前提条件
4. 手順
5. 実例
6. よくあるエラー / 注意点
7. まとめ

テーマに不要な章は無理に入れない。

見出しルール:
- H1は書かない
- H2を章
- H3を節
- H4以下は極力使わない
- 見出しだけ読んでも流れが分かるようにする

悪い見出し:
- 手順
- 設定
- 注意点

良い見出し:
- WordPressでApplication Passwordを発行する
- `.env` にWordPressの接続情報を設定する
- 401エラーが出たときに確認する3項目

## 7. コード・コマンド

- 原則コピペ可能にする
- コードフェンスには言語を指定する
- WindowsとmacOS/Linuxで異なる場合は分ける
- 実在の秘密情報は使わない
- URL、ユーザー名、APIキーはプレースホルダーを使う
- 破壊的操作には注意書きを付ける
- 省略すると動かなくなるコードは省略しない
- 本文とコードでファイル名・変数名を一致させる

## 8. WordPress用Front Matter

新規記事では原則以下を使う。

```yaml
---
title: ""
slug: ""
excerpt: ""
status: "draft"
categories: []
tags: []
featured_image: ""
featured_image_alt: ""
---
```

既存記事に以下があれば保持する。

```yaml
wordpress_post_id:
wordpress_url:
wordpress_featured_media_id:
```

### title
- 何の記事か一目で分かる
- 検索意図と一致する
- 煽りを避ける
- 本文と一致させる

### slug
- 英小文字・数字・ハイフン
- 短く意味が分かる
- 日付や無意味な連番は原則使わない

例:
```text
codex-wordpress-rest-api
```

### excerpt
- 記事の要約
- 読者が何を得られるかを書く
- タイトルの単純な言い換えにしない
- 100〜160文字程度を目安にする

### categories
大分類として使い、原則1〜2個。

### tags
記事内の技術要素や製品名を付け、増やしすぎない。

## 9. SEO方針

優先順位:
1. 検索意図
2. 読みやすさ
3. 正確性
4. 網羅性
5. キーワード

記事作成時は可能なら以下を整理する。
- 想定読者
- 主キーワード
- 読者の疑問
- 読後にできること
- 競合記事との差別化

キーワードを不自然に反復しない。

## 10. 技術記事の事実確認

特に変化しやすい情報:
- OpenAI / Codex / ChatGPT
- WordPress
- ConoHa WING等のホスティング
- プラグイン
- SaaS
- API仕様
- ライブラリ
- Python / Node.js等
- 料金
- UI
- 法律
- サービス提供地域

確認できる場合は公式ドキュメント・公式ヘルプ・公式GitHubを優先する。
確認できない場合は断定しない。

## 11. WordPress投稿フロー

1. `AGENTS.md` を確認
2. `config/blog.yaml` を確認
3. `prompts/article.md` を確認
4. `articles/drafts/` にMarkdownを作成
5. Front Matterを確認
6. `prompts/review.md` に沿ってレビュー・修正
7. 必要なら `--dry-run`
8. ユーザー指示に従ってWordPressへ送信

dry-run:
```bash
python scripts/wp_publish.py articles/drafts/example.md --dry-run
```

下書き送信:
```bash
python scripts/wp_publish.py articles/drafts/example.md
```

公開:
```bash
python scripts/wp_publish.py articles/drafts/example.md --publish
```

`--publish` は明示的な公開指示がある場合のみ使用する。

## 12. 既存記事の更新

- `wordpress_post_id` を保持する
- URLを不用意に変更しない
- slug変更はSEO影響があるため慎重に扱う
- タイトル変更時は検索意図が変わっていないか確認する
- 古い情報を更新したら周辺記述も整合させる
- 更新後に重複説明が残っていないか確認する

## 15. Codexの作業ルール

- 既存ファイルを読む前に決めつけない
- 必要以上に大規模な変更をしない
- 小さく安全な変更を優先する
- 投稿スクリプト変更後は最低限構文確認する
- エラーを隠さない
- 実行できなかった検証を「実行した」と書かない
- 最新のユーザー指示を優先する

## 16. Build in Public専用ルール

このブログの中心テーマは、個人開発中WebアプリのBuild in Publicである。
記事作成時は `CONTENT_STRATEGY.md` も読む。

主な記事タイプ:
- Build in Public
- 課題と解決
- 開発の裏側
- アプリ比較

### Build in Public記事

単なる進捗報告にしない。
最低でも「判断理由・失敗・検証結果・再利用できる学び」のいずれかを含める。

### 悩み解決記事

必ず次を分けて考える。
- 悩みそのもの
- 現在の対処法
- 既存手段の弱点
- 開発中アプリの解決方法
- 解決できない範囲

### 他アプリで解決できるかの検証

同じ悩みを既存アプリでも解決できる場合は、その事実を隠さない。
比較記事では原則として有力候補を最大3つ挙げる。

比較基準:
- 悩みへの適合度
- 導入しやすさ
- 料金
- 使いやすさ
- プライバシー
- 継続利用しやすさ

自分のアプリを自動的に1位にしない。
候補が3つ未満なら水増ししない。
最新の製品情報が必要な場合は、可能なら公式情報を確認する。

## WordPress記事の読者

WordPressの記事は一般ユーザー向けとする。

- 技術知識を前提にしない。
- 専門用語はできるだけ避ける。
- 必要な専門用語は、先に普通の言葉で説明してから補足する。
- コードや実装詳細より、「どんな悩みがあるか」「どう楽になるか」「誰に向いているか」を優先する。
- 開発中アプリの宣伝だけにならないよう、既存の解決方法や他アプリも公平に扱う。
